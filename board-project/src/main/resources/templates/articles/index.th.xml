<?xml version="1.0"?>
<thlogic>
    <attr sel="#header" th:replace="header :: header"/> <!-- 전체 문서중 id가 header인 녀석을 찾는다, 다행히 index.html 에 header 는 하나 밖에 없는데
                                이 방법이 불안하면 index.html 의 header에 id를 부여하고 여기서 attr sel="#id명"을 사용해라-->
    <attr sel="#footer" th:replace="footer :: footer"/> <!-- footer html의 footer를 찾아라-->

    <attr sel="#article-table">
        <attr sel="tbody" th:remove="all-but-first">
            <attr sel="tr[0]" th:each="article : ${articles}">
                <attr sel="td.title/a" th:text="${article.title}" th:href="@{'/articles/' + ${article.id}}"/>
                <attr sel="td.hashtag" th:text="${article.hashtag}" />
                <attr sel="td.user-id" th:text="${article.nickname}" />
                <attr sel="td.created-at/time" th:datetime="${article.createdAt}" th:text="${#temporals.format(article.createdAt, 'yyyy-MM-dd')}" />
            </attr>
        </attr>
    </attr>
</thlogic>